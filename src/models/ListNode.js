class ListNode {
      constructor(value) {
      // Initializes the value stored in the node
      this.value = value;
      // Initializes the 'next' pointer to null, indicating the node is not connected to another node by default
      this.next = null;
    }
  }
  
  module.exports = { ListNode }; // Exports the class
  