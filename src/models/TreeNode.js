class TreeNode {
    constructor(value) {
      // Initializes the value stored in the node
      this.value = value;
      // Initializes references to left and right child nodes (initially set to null)
      this.left = null;
      this.right = null;
    }
  }
  
  module.exports = { TreeNode }; // Exports the class
  